[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
addopts =
    -ra
    --strict-markers
    --strict-config
markers =
    integration: requires real Supabase/IG credentials (skipped by default)
