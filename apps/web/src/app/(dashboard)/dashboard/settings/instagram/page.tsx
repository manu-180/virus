import InstagramAccountsClient from './_components/InstagramAccountsClient';

export const metadata = {
  title: 'Instagram | Configuración',
};

export default function InstagramSettingsPage() {
  return <InstagramAccountsClient />;
}
